import React from "react";

// Imports CSS Content from page.module.css
// To Style, you put in a tag 'className={styles.(CSS Name)}
import styles from "@/app/page.module.css";

// Tip: Use the <footer> tag and design your CSS in the global to help design and keep your code readable

export default function Footer() {
  return (
    <div>
      <h1>Footer</h1>
    </div>
  );
}
