import React from "react";

// Imports CSS Content from page.module.css
// To Style, you put in a tag 'className={styles.(CSS Name)}
import styles from "@/app/page.module.css";

// Tip: Use the <nav> tag and design your CSS in the global to help design and keep your code readable
// To display an image, use the NextJS guidelines when displaying
export default function Navigation() {
  return (
    <div>
      <h1>Navigation</h1>
    </div>
  );
}
